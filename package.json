{
  "name": "pdf-slot-design",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "lint": "npm run lint --workspace=web && npm run lint --workspace=api",
    "test": "npm test --workspace=@pdf-slot/core && npm test --workspace=@pdf-slot/contracts && npm test --workspace=api && npm run test:e2e --workspace=api && npm test --workspace=web",
    "typecheck": "npm run typecheck --workspace=@pdf-slot/core && npm run typecheck --workspace=@pdf-slot/contracts && npm run typecheck --workspace=api && npm run typecheck --workspace=web",
    "verify": "npm run lint && npm run typecheck && npm test && npm run build"
  }
}
